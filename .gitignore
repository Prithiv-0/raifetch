# Build output
/target
*.tmp

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment / secrets
.env
.env.*

# Rust-specific
**/*.rs.bk
*.orig

# raifetch runtime cache (lives in /tmp, but just in case)
raifetch_pkg_*

# AUR build artifacts and nested clone
aur/
aur/pkg/
aur/src/
aur/*.pkg.tar.zst
aur/*.src.tar.gz
aur/*.log
aur/*.tar.gz
aur/raifetch/

# Local release archives
raifetch-*.tar.gz
